package towssome.server.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface CategoryClassificationRepository extends JpaRepository<CategoryClassificationRepository, Long> {
}
